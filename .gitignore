# Go
/bin/
*.test
*.out

# Embedded web build (generated by `make web` / Docker); keep only .gitkeep
internal/server/dist/*
!internal/server/dist/.gitkeep

# Web
web/node_modules/
web/dist/
web/.output/
web/.nitro/
web/.tanstack/

# Server SQLite store (default --db path) and WAL sidecars
envvar.db
envvar.db-shm
envvar.db-wal

# envvar's own secrets, should this repo ever use them
.env.keys
.env.local
.env.*.local

# Editor/OS
.DS_Store
